memory_limit=${PHP_MEMORY_LIMIT}
upload_max_filesize=${PHP_UPLOAD_MAX_FILESIZE}
post_max_size=${PHP_POST_MAX_SIZE}
max_execution_time=300
date.timezone=UTC

; Recommended for production containers; set CI_ENV=development to show CI errors via app config.
display_errors=0
log_errors=1

